# Git Workflow

- The `main` branch is protected and contains stable code.
- Each task will be completed in a separate branch.
- Branch naming format: feature/task-name
- Commit messages should be clear and descriptive.
- Pull requests will be reviewed by at least one team member before merging.
